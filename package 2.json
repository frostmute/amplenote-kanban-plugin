{
  "name": "amplenote-kanban-plugin",
  "version": "1.0.0",
  "description": "Markdown-backed Kanban boards for Amplenote",
  "main": "build/compiled.js",
  "scripts": {
    "build": "node esbuild.js",
    "watch": "nodemon -e ts -x 'npm run build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frostmute/amplenote-kanban-plugin.git"
  },
  "author": "Jonathan J. Wagner",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.0.0"
  }
}
